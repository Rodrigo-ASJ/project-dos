const unuseed_var = "ManzDev";
let url = "https://manz.dev";

url = "esto no se puede hacer";

console.log(url, unuseed_var);

function name(cosa) {
  const otro = "name" + cosa;
  return otro;
}

name();
